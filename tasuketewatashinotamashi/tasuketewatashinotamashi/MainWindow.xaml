<Window x:Class="tasuketewatashinotamashi.MainWindow"
      
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:tasuketewatashinotamashi"
        mc:Ignorable="d" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DesignHeight="600" d:DesignWidth="900"
        Title="MainWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button Content="Add" Click="AddPerson_Click" ToolTip="Add new employee"/>
            <Button Content="Edit" Click="EditPerson_Click" ToolTip="Edit selected employee"/>
            <Button Content="Delete" Click="DeletePerson_Click" ToolTip="Delete selected employee"/>
            <Separator/>
            <Button Content="Refresh" Click="Refresh_Click" ToolTip="Refresh data"/>
            <Separator/>
            <ComboBox Name="FilterComboBox" Width="150" SelectionChanged="FilterComboBox_SelectionChanged">
                <ComboBoxItem Content="All employees"/>
                <ComboBoxItem Content="On business trip"/>
                <ComboBoxItem Content="By organisation"/>
            </ComboBox>
        </ToolBar>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1" Name="PersonsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                 SelectionMode="Single" SelectionChanged="PersonsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Middle Name" Binding="{Binding MiddleName}" Width="120"/>
                <DataGridTextColumn Header="Birth Date" Binding="{Binding BirthDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Position.Name}" Width="150"/>
                <DataGridTextColumn Header="Subunit" Binding="{Binding Position.Subunit.Name}" Width="150"/>
                <DataGridTextColumn Header="Organisation" Binding="{Binding Position.Subunit.Organisation.Name}" Width="150"/>
                <DataGridTextColumn Header="Business Trip" Binding="{Binding BusinessTrip.Description}" Width="150"/>
                <DataGridTextColumn Header="Work Book" Binding="{Binding WorkBook.Number}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Details Panel -->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Personal Info -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Personal Information" FontWeight="Bold"/>
                    <TextBlock>
                        <Run Text="Full Name: "/>
                        <Run Text="{Binding SelectedItem.LastName, ElementName=PersonsGrid}"/>
                        <Run Text="{Binding SelectedItem.FirstName, ElementName=PersonsGrid}"/>
                        <Run Text="{Binding SelectedItem.MiddleName, ElementName=PersonsGrid}"/>
                    </TextBlock>
                    <TextBlock Text="{Binding SelectedItem.BirthDate, ElementName=PersonsGrid, StringFormat='Birth Date: \{0:dd.MM.yyyy\}'}"/>

                    <TextBlock>
                        <Run Text="Address: "/>
                        <Run Text="{Binding SelectedItem.Street.Name, ElementName=PersonsGrid}"/>
                        <Run Text=", "/>
                        <Run Text="{Binding SelectedItem.Street.City.Name, ElementName=PersonsGrid}"/>
                    </TextBlock>
                </StackPanel>

                <!-- Work Info -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Work Information" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SelectedItem.Position.Name, ElementName=PersonsGrid, StringFormat='Position: \{0\}'}"/>
                    <TextBlock Text="{Binding SelectedItem.Position.Subunit.Name, ElementName=PersonsGrid, StringFormat='Subunit: \{0\}'}"/>
                    <TextBlock Text="{Binding SelectedItem.Position.Subunit.Organisation.Name, ElementName=PersonsGrid, StringFormat='Organisation: \{0\}'}"/>
                    <TextBlock Text="{Binding SelectedItem.WorkBook.Number, ElementName=PersonsGrid, StringFormat='Work Book: \{0\}'}"/>
                    <TextBlock Text="{Binding SelectedItem.WorkBook.Speciality.Name, ElementName=PersonsGrid, StringFormat='Speciality: \{0\}'}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>

</Window>
