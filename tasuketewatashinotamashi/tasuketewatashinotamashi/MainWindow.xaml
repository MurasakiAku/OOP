<Window x:Class="tasuketewatashinotamashi.MainWindow"
      
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:tasuketewatashinotamashi"
        mc:Ignorable="d" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DesignHeight="600" d:DesignWidth="900"
        Title="MainWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Label Grid.Row="0" Content="Employee Management System" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>

        <!-- Основное содержимое -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Список сотрудников -->
            <GroupBox Grid.Column="0" Header="Employees" Margin="5">
                <DataGrid ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding EmployeeId}" Width="Auto"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding LastName}" Width="*"/>
                        <DataGridTextColumn Header="Position" Binding="{Binding Position.Title}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Форма редактирования -->
            <GroupBox Grid.Column="1" Header="Employee Details" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="First Name:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEmployee.FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Last Name:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEmployee.LastName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="Middle Name:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEmployee.MiddleName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Birth Date:"/>
                    <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding SelectedEmployee.BirthDate, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <Label Grid.Row="4" Grid.Column="0" Content="Phone:"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedEmployee.PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <Label Grid.Row="5" Grid.Column="0" Content="Email:"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding SelectedEmployee.Email, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <Label Grid.Row="6" Grid.Column="0" Content="Position:"/>
                    <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding Positions}" DisplayMemberPath="Title" SelectedValuePath="PositionId" SelectedValue="{Binding SelectedEmployee.PositionId}" Margin="5"/>

                    <Label Grid.Row="7" Grid.Column="0" Content="Department:"/>
                    <ComboBox Grid.Row="7" Grid.Column="1" ItemsSource="{Binding Departments}" DisplayMemberPath="Name" SelectedValuePath="DepartmentId" SelectedValue="{Binding SelectedEmployee.DepartmentId}" Margin="5"/>

                    <Label Grid.Row="8" Grid.Column="0" Content="Specialization:"/>
                    <ComboBox Grid.Row="8" Grid.Column="1" ItemsSource="{Binding Specializations}" DisplayMemberPath="Name" SelectedValuePath="SpecializationId" SelectedValue="{Binding SelectedEmployee.SpecializationId}" Margin="5"/>

                    <!-- Кнопки -->
                    <StackPanel Grid.Row="9" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                        <Button Content="Add" Command="{Binding AddCommand}" Width="80" Margin="5"/>
                        <Button Content="Update" Command="{Binding UpdateCommand}" Width="80" Margin="5"/>
                        <Button Content="Delete" Command="{Binding DeleteCommand}" Width="80" Margin="5"/>
                        <Button Content="Clear" Command="{Binding ClearCommand}" Width="80" Margin="5"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2">
            <StatusBarItem Content="Ready"/>
        </StatusBar>
    </Grid>
</Window>
